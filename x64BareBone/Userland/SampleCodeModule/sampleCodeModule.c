#include <stdLib.h>

int main() {
	//All the following code may be removed 
/*	
	char c;

	while (c = getChar())
	{
		putChar(c);
	}
	*/
	char output[20];
	scanf(output, 20);
	printf(output);

	//Test if BSS is properly set up
	return 0;
}
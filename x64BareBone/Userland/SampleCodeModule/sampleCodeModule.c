#include <stdLib.h>
#include <shell.h>
#include <arkanoid.h>

int main() {
	//All the following code may be removed 
/*	
	char c;

	while (c = getChar())
	{
		putChar(c);
	}
	*/
	startArkanoid();
	return 1;
}
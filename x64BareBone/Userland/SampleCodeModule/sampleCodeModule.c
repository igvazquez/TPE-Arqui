#include <stdLib.h>
#include <shell.h>

int main() {
	//All the following code may be removed 
/*	
	char c;

	while (c = getChar())
	{
		putChar(c);
	}
	*/
	return startShell();
	//Test if BSS is properly set up
}
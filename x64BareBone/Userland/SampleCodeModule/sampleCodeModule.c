#include <stdLib.h>

static int var1 = 0;
static int var2 = 0;


int main() {
	//All the following code may be removed 
/*	
	char c;

	while (c = getChar())
	{
		putChar(c);
	}
	*/
	char output[20];
	scanf(output, 20);
	printf(output);

	//Test if BSS is properly set up
	return 0;
}